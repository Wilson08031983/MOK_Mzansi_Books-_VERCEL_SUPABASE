// Execute this in the browser console to create sample employees
console.log('🚀 Executing sample employee creation...');

// Check if the function is available
if (typeof createSampleEmployeesWithData === 'function') {
  createSampleEmployeesWithData();
} else {
  console.error('❌ createSampleEmployeesWithData function not found. Please refresh the page and try again.');
}
